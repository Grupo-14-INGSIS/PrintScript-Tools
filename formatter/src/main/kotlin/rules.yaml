# ej de onfiguración para formatter
rules:
  switch:
    SpaceAroundEquals: true
    # NoSpaceBeforeEquals: false  # Ya no necesitas estas
    # NoSpaceAfterEquals: false

  setValue:
    lineBreakBeforePrint: 1    # 0, 1 o 2 saltos de línea antes de println
    indentSize: 2              # espacios por nivel de indentación